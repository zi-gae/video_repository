extends layouts/main.pug
include mixins/videoBlock
block content
    .search__header
        h3 Searching : #{searchingBy}
    .search__videos
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.fileffUrl,
                id:item.id
            })